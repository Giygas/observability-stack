# ── Alloy Mode ─────────────────────────────────────────────
# Local mode (default): config.alloy - obs stack runs as submodule
# Remote mode: config.remote.alloy - obs stack on remote machine via tunnel
# Default is always local — remote requires explicit flag
ALLOY_CONFIG=config.alloy

# ── Grafana ────────────────────────────────────────────────
GRAFANA_ADMIN_USER=admin
GRAFANA_PORT=3000

# ── Prometheus ────────────────────────────────────────────
PROMETHEUS_PORT=9090
PROMETHEUS_RETENTION=720h        # 30 days

# ── Loki ──────────────────────────────────────────────────
LOKI_PORT=3100

# ── Cloudflare Tunnel ─────────────────────────────────────
# Get from: Cloudflare Zero Trust → Tunnels → Create tunnel
# Configure these public hostnames in the tunnel dashboard:
#   prometheus-obs.yourdomain.com → http://prometheus:9090
#   loki-obs.yourdomain.com       → http://loki:3100
#   grafana-obs.yourdomain.com    → http://grafana:3000
CLOUDFLARE_TUNNEL_TOKEN=

# If using Cloudflare Access to protect endpoints (recommended)
# Get from: Zero Trust → Access → Service Auth → Service Tokens
CF_ACCESS_CLIENT_ID=
CF_ACCESS_CLIENT_SECRET=

# ── Tailscale ─────────────────────────────────────────────
# Get from: tailscale.com → Settings → Auth Keys
TAILSCALE_AUTHKEY=
TS_HOSTNAME=obs-stack

# ── WireGuard ─────────────────────────────────────────────
WG_SERVER_URL=                   # Your server public IP or domain
WG_PORT=51820
WG_PEERS=1
